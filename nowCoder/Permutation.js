function Permutation(str) {
    let arr = str.split('');

    console.log(arr);
}
function swap(arr,res,tmp){
    let len = arr.length;
    let i, j;
    while (i < len){
        tmp.l
    }
}

Permutation('abc')